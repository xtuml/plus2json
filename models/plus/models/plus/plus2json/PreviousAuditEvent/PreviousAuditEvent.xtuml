// BP 7.1.6 content: ModelClass syschar: 3 persistence-version: 7.1.6

within plus::plus2json is

  @key_letters("PreviousAuditEvent");
  @class_num(4);
  class PreviousAuditEvent is

    previous_event: instance of AuditEvent;

    ConstraintDefinitionId: string;

    ConstraintValue: string;

    @dialect("oal");
    @operation_num(1);
    operation json(pdelim: in string) return string;

  end class;

end;
