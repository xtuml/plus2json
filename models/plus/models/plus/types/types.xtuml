// BP 7.1.6 content: Package syschar: 3 persistence-version: 7.1.6

within plus is

  package types is
    type ConstraintType is enum (
      AND, 
      XOR, 
      IOR
    );

    type EventDataType is enum (
      EINV, 
      IINV, 
      LCNT, 
      BCNT, 
      MCNT
    );

    type UUID is string;

  end package;

end;
